# Лабораторная работа №4 — Архитектура компьютера

**Студент:** Новиков Даниил Дмитриевич  
**Группа:** P3231  
**Проверил:** Пенской Александр Владимирович  
**Вариант:** `lisp | stack | harv | mc | tick | binary | stream | port | pstr | prob1 | superscalar`

---

## Оглавление

- [1. Архитектура процессора](#1-архитектура-процессора)
- [2. Язык программирования](#2-язык-программирования)
  - [2.1. Общая идея](#21-общая-идея)
  - [2.2. Расширенная форма Бэкуса-Наура](#22-расширенная-форма-бэкуса-наура-)
  - [2.3. Литералы и escape-последовательности](#23-литералы-и-escape-последовательности)
  - [2.4. Семантика вычислений](#24-семантика-вычислений)
  - [2.5. Типизация](#25-типизация)
- [3. Строки: хранение и работа с pstr](#3-строки-хранение-и-работа-с-pstr)
  - [3.1. Формат хранения строки](#31-формат-хранения-строки)
  - [3.2. Что возвращает строковый литерал](#32-что-возвращает-строковый-литерал)
  - [3.3. Печать строки](#33-печать-строки)
  - [3.4. Ограничения строк](#34-ограничения-строк)
- [4. Память](#4-память)
  - [4.1. Гарвардская архитектура](#41-гарвардская-архитектура)
  - [4.2. Память команд](#42-память-команд)
  - [4.3. Память данных](#43-память-данных)
  - [4.4. Переменные](#44-переменные)
  - [4.5. Служебные переменные транслятора](#45-служебные-переменные-транслятора)
- [5. Система команд ISA](#5-система-команд-isa)
  - [5.1. Формат инструкции](#51-формат-инструкции)
  - [5.2. Набор инструкций](#52-набор-инструкций)
  - [5.3. Порты ввода-вывода](#53-порты-ввода-вывода)
- [6. Транслятор](#6-транслятор)
  - [6.1. Назначение](#61-назначение)
  - [6.2. Этапы трансляции](#62-этапы-трансляции)
  - [6.3. Пример трансляции `setq`](#63-пример-трансляции-setq)
  - [6.4. Процедуры](#64-процедуры)
  - [6.5. Условия](#65-условия)
  - [6.6. Цикл](#66-цикл)
- [7. Машина и микрокод](#7-машина-и-микрокод)
  - [7.1. `isa.py`](#71-isapy)
  - [7.2. `microcode.py`](#72-microcodepy)
  - [7.3. `machine.py`](#73-machinepy)
  - [7.4. DataPath](#74-datapath)
  - [7.5. ControlUnit](#75-controlunit)
- [8. Тестирование](#8-тестирование)
  - [0. Формат golden.yml](#0-формат-goldenyml-файлов)
  - [1. hello](#1-hello)
  - [2. cat](#2-cat)
  - [3. hello_user_name](#3-hello_user_name)
  - [4. sort](#4-sort)
  - [5. double_precision](#5-double_precision)
  - [6. prob1](#6-prob1)
  - [7. show_string_methods](#7-show_string_methods)
  - [8. factorial](#8-factorial)
  - [9. interrupt_test](#9-interrupt_test)
- [9. Файлы, создаваемые при запуске](#9-файлы-создаваемые-при-запуске)
- [10. Интерфейс командной строки](#10-интерфейс-командной-строки)
  - [10.1. Транслятор](#101-транслятор)
  - [10.2. Машина](#102-машина)
  - [10.3. Скрипт полного запуска](#103-скрипт-полного-запуска)
- [11. Формат дизассемблера](#11-формат-дизассемблера)
- [12. Журнал выполнения](#12-журнал-выполнения)

## 1. Архитектура процессора:

- стековая архитектура;
- гарвардская организация памяти: память команд и память данных разделены;
- байтовая адресация памяти команд и памяти данных;
- однопортовая память данных;
- микропрограммное управление;
- port-mapped ввод-вывод;
- поддержка аппаратных и программных прерываний;
- строковые литералы представлены в памяти данных в формате `pstr` — Pascal string.

---

## 2. Язык программирования

### 2.1. Общая идея

Язык является Lisp-подобным. Программа состоит из последовательности S-выражений.

Пример программы:

```lisp
(setq x 10)
(print-int (+ x 5))
(print-char 10)
```

После разбора парсер превращает исходный код в обычные Python-структуры.

Например:

```lisp
(+ 1 (* 2 3))
```

превращается в:

```python
["+", 1, ["*", 2, 3]]
```

---

### 2.2. Расширенная форма Бэкуса-Наура 

```bnf
program          ::= expression*

expression       ::= atom
                   | "(" list_expr ")"

atom             ::= integer
                   | symbol
                   | string_literal

integer          ::= ["-"] digit+
symbol           ::= (letter | special_char) (letter | digit | special_char)*
string_literal   ::= '"' char* '"'

list_expr        ::= form_setq
                   | form_if
                   | form_loop
                   | form_begin
                   | form_proc
                   | form_call
                   | form_interrupt
                   | form_arithmetic
                   | form_comparison
                   | form_io
                   | form_memory
                   | form_pstr

form_setq        ::= "setq" symbol expression
form_if          ::= "if" expression expression expression
form_loop        ::= "loop" expression expression*
form_begin       ::= "begin" expression*
form_proc        ::= "proc" symbol ["(" symbol* ")"] expression*
form_call        ::= "call" symbol expression*

form_interrupt   ::= "interrupt" expression*
                   | "enable-interrupts"
                   | "disable-interrupts"
                   | "int"

form_arithmetic  ::= ("+" | "-" | "*" | "/" | "%") expression expression+
form_comparison  ::= ("=" | "!=" | "<" | ">" | "<=" | ">=") expression expression

form_io          ::= "print" expression
                   | "print-pstr" expression
                   | "print-int" expression
                   | "print-char" expression
                   | "read-int"
                   | "read-char"

form_memory      ::= "load-at" expression
                   | "store-at" expression expression

form_pstr        ::= "pstr-len" expression
                   | "pstr-get" expression expression
                   | "pstr-set" expression expression expression

special_char     ::= "-" | "_" | "+" | "*" | "/" | "%" | "=" | "!" | "<" | ">" | "?"
digit            ::= "0".."9"
letter           ::= "a".."z" | "A".."Z"
```

Комментарии начинаются с `;` и продолжаются до конца строки.

Пример:

```lisp
; это комментарий
(setq x 1) ; это тоже комментарий
```

---

### 2.3. Литералы и escape-последовательности

Поддерживаются целые числа, символы и строковые литералы.

```lisp
42
-7
x
"Hello"
"Hello\n"
```

В строках поддерживаются escape-последовательности:

| Последовательность | Значение |
|---|---|
| `\n` | перевод строки |
| `\t` | табуляция |
| `\"` | кавычка |
| `\\` | обратный слэш |

Строки предполагаются ASCII-строками. Один символ хранится в одном машинном слове памяти данных, но при выводе через порт используется младший байт значения.

---

### 2.4. Семантика вычислений

Язык использует expression semantics: почти каждая конструкция оставляет результат на вершине стека.

| Конструкция | Результат |
|---|---|
| `(setq x expr)` | значение `expr` |
| `(if cond then else)` | результат выбранной ветки |
| `(loop cond body...)` | `0` после завершения цикла |
| `(begin e1 e2 ... en)` | результат последнего выражения |
| `(proc name body...)` / `(proc name (args...) body...)` | не выполняется как обычный main-код, а размещается в памяти команд |
| `(call name arg1 arg2 ...)` | результат последнего выражения процедуры |
| `(print expr)` | `0` |
| `(print-pstr expr)` | `0` |
| `(print-int expr)` | исходное значение `expr` |
| `(print-char expr)` | исходное значение `expr` |
| `(read-char)` | код прочитанного символа |
| `(read-int)` | прочитанное целое число |
| `(pstr-len s)` | длина pstr-строки |
| `(pstr-get s i)` | код символа по индексу `i` |
| `(pstr-set s i v)` | `0` |
| `(load-at addr)` | значение из памяти данных по адресу `addr` |
| `(store-at addr value)` | `0` |
| `(enable-interrupts)` | `0` |
| `(disable-interrupts)` | `0` |
| `(int)` | `0` после программного прерывания |

Обычные вычислительные формы вычисляют аргументы слева направо. Специальные формы имеют собственную семантику:

- `if` вычисляет условие и только одну из веток;
- `loop` вычисляет условие перед каждой итерацией;
- `begin` выполняет выражения последовательно;
- `proc` описывает процедуру, но не выполняет её при старте;
- `interrupt` описывает обработчик прерывания.

---

### 2.5. Типизация

Единственный машинный тип данных — 32-битное знаковое целое число.

Строки не являются отдельным машинным типом. Строковый литерал при трансляции размещается в памяти данных как `pstr`, а в выражении используется его адрес.

Логические значения:

```text
0        — ложь
не 0     — истина
```

Пример:

```lisp
(if (> x 0)
  (print "positive")
  (print "not positive"))
```

---

## 3. Строки: хранение и работа с pstr

### 3.1. Формат хранения строки

Строки хранятся в памяти данных в формате Pascal string.

Строка:

```lisp
"Hi"
```

размещается так:

```text
addr + 0 : 2       ; длина строки
addr + 4 : 72      ; 'H'
addr + 8 : 105     ; 'i'
```

Общий формат:

```text
addr + 0     : N               ; длина строки
addr + 4     : char_0
addr + 8     : char_1
...
addr + 4 * N : char_(N - 1)
```

Каждый символ занимает одно 32-битное слово памяти данных, то есть 4 байта. Адреса в программе являются байтовыми, поэтому соседние символы pstr располагаются с шагом `4`. При выводе символа в порт используется младший байт.

Максимальная длина строки — 255 символов.

---

### 3.2. Что возвращает строковый литерал

Когда в программе встречается строковый литерал:

```lisp
"Hello"
```

транслятор:

1. выделяет в памяти данных блок `N + 1` слов;
2. кладёт в первую ячейку длину строки;
3. кладёт дальше ASCII-коды символов;
4. в машинный код вставляет `PUSHI addr`, где `addr` — адрес начала pstr.

То есть выражение:

```lisp
(setq s "Hi")
```

компилируется по смыслу так:

```text
PUSHI <addr_of_Hi>
DUP
STORE <addr_of_s>
```

Переменная `s` хранит не саму строку, а адрес pstr-структуры.

---

### 3.3. Печать строки

Формы:

```lisp
(print "Hello")
(print-pstr "Hello")
```

эквивалентны по реализации. Обе ожидают, что выражение вернёт адрес pstr.

Пример:

```lisp
(setq s "Hello")
(print s)
```

Алгоритм печати строки:

```text
ptr = pstr_addr
len = memory[ptr]
ptr = ptr + 4

while len > 0:
    OUT 1, memory[ptr]
    ptr = ptr + 4
    len = len - 1

push 0
```

В ISA нет отдельной инструкции `PRINT_STRING`. Транслятор вставляет код печати строки: чтение длины, цикл, косвенное чтение через `LOADI`, вывод каждого символа через `OUT 1`.

---

### 3.4. Ограничения строк

- строки имеют фиксированную длину после размещения в памяти данных;
- нет динамического выделения памяти во время выполнения;
- строка — это адрес, поэтому `(print x)` корректно работает только если `x` содержит адрес pstr;
- для печати числа нужно использовать `print-int`, а для одного символа — `print-char`.

---

## 4. Память

### 4.1. Гарвардская архитектура

В модели используется гарвардская архитектура: память команд и память данных разделены.

Программист не может читать или менять память команд из программы. Данные читаются и записываются только через память данных и стек.

---

### 4.2. Память команд

Память команд содержит инструкции ISA. Каждая инструкция занимает одно 32-битное слово, то есть 4 байта.

Адресация байтовая: `PC` хранит байтовый адрес текущей инструкции, а следующий последовательный адрес получается как `PC + 4`.

```text
0 : JMP main_start
4 : код процедуры
...
k : код обработчика прерывания
...
n : основной код программы
...
m : HALT
```

В начало программы транслятор всегда вставляет `JMP main_start`. Это нужно, чтобы при запуске процессор сразу перешёл к основному коду и не начал выполнять тела процедур или обработчик прерывания как обычные инструкции.

---

### 4.3. Память данных

Память данных имеет байтовую адресацию. При этом машинное слово данных — 32-битное знаковое целое число, которое занимает 4 байта. Инструкции `LOAD`, `STORE`, `LOADI`, `STOREI` работают со словом целиком: читают или записывают 4 байта, начиная с указанного байтового адреса. Адрес слова должен быть выровнен по 4 байтам.

Память данных однопортовая: в один такт выполняется не более одного обращения к памяти данных.

Зарезервированная область:

```text
0x00..0x03 : interrupt vector       ; адрес обработчика прерывания в памяти команд
0x04..0x07 : interrupt device cell  ; символ от внешнего устройства при IRQ
0x08       : первая свободная ячейка для пользовательских данных
```

Общая схема:

```text
Data Memory
+---------------------------------------+
| 0x00..0x03 : interrupt vector         |
| 0x04..0x07 : interrupt device cell    |
| 0x08       : первая свободная ячейка  |
| ...                                   |
| s          : длина pstr               |
| s + 4      : первый символ            |
| s + 8      : второй символ            |
| ...                                   |
| v          : переменная x             |
| v + 4      : переменная y             |
| ...                                   |
| tmp        : служебные переменные     |
+---------------------------------------+
```

Файлы `.data.json` и `.data.out.json` хранят память данных в байтовом виде: каждый элемент JSON-массива — один байт от `0` до `255`. Одно машинное слово кодируется четырьмя последовательными байтами в формате little-endian. Например, число `300` хранится как `[44, 1, 0, 0]`.

---

### 4.4. Переменные

Все переменные глобальные. При первом обращении к имени транслятор выделяет ему одну ячейку в памяти данных.

Пример:

```lisp
(setq x 1)
```

Если `x` получает адрес `8`, то генерируется примерно такой код:

```text
PUSHI 1
DUP
STORE 8
```

`DUP` нужен потому, что `setq` является выражением и должен оставить записанное значение на стеке.

Чтение переменной:

```lisp
x
```

компилируется как:

```text
LOAD <addr_of_x>
```

---

### 4.5. Служебные переменные транслятора

Некоторые операции языка разворачиваются в длинный inline-код. Для него транслятор выделяет служебные переменные в памяти данных.

Примеры:

```text
__pstr_ptr
__pstr_len
__read_int_ch
__read_int_value
__read_int_sign
__proc_<name>_<param>
__call_result
```

Они хранятся в той же памяти данных, что и пользовательские переменные.

---

## 5. Система команд ISA

### 5.1. Формат инструкции

Каждая инструкция кодируется в 32-битное машинное слово:

```text
31      24 23                    0
+----------+----------------------+
|  opcode  |       operand        |
|  8 bit   |       24 bit         |
+----------+----------------------+
```

- `opcode` — код операции;
- `operand` — операнд инструкции;
- для `PUSHI` операнд знаковый 24-битный;
- для адресных инструкций операнд беззнаковый и задаёт байтовый адрес;
- инструкции без операнда имеют `operand = 0`.

Диапазон для `PUSHI`:

```text
-8388608 .. 8388607
```

---

### 5.2. Набор инструкций

В таблице указаны фактические такты успешного выполнения инструкции с учётом `FETCH`.

| Инструкция | Hex | Операнд | Стековый эффект / смысл | Тактов |
|---|---:|---|---|-------:|
| `HALT` | `0x00` | — | остановить процессор |      2 |
| `PUSHI` | `0x01` | `imm` | `-- value` |      4 |
| `DROP` | `0x02` | — | `a --` |      4 |
| `DUP` | `0x03` | — | `a -- a a` |      4 |
| `LOAD` | `0x10` | `addr` | `-- mem[addr]` |      4 |
| `STORE` | `0x11` | `addr` | `value --` |      4 |
| `LOADI` | `0x12` | — | `addr -- mem[addr]` |      4 |
| `STOREI` | `0x13` | — | `addr value --` |      4 |
| `ADD` | `0x20` | — | `b a -- b+a` |      6 |
| `SUB` | `0x21` | — | `b a -- b-a` |      6 |
| `MUL` | `0x22` | — | `b a -- b*a` |      6 |
| `DIV` | `0x23` | — | `b a -- b/a` |      6 |
| `MOD` | `0x24` | — | `b a -- b%a` |      6 |
| `EQ` | `0x30` | — | `b a -- int(b==a)` |      6 |
| `NE` | `0x31` | — | `b a -- int(b!=a)` |      6 |
| `LT` | `0x32` | — | `b a -- int(b<a)` |      6 |
| `GT` | `0x33` | — | `b a -- int(b>a)` |      6 |
| `LE` | `0x34` | — | `b a -- int(b<=a)` |      6 |
| `GE` | `0x35` | — | `b a -- int(b>=a)` |      6 |
| `JMP` | `0x40` | `addr` | `PC = addr` |      2 |
| `JZ` | `0x41` | `addr` | `cond --`, переход если `cond == 0` |      2 |
| `JNZ` | `0x42` | `addr` | `cond --`, переход если `cond != 0` |      2 |
| `CALL` | `0x50` | `addr` | сохранить `PC+4`, перейти |      2 |
| `RET` | `0x51` | — | вернуться по адресу из return stack |      2 |
| `IN` | `0x60` | `port` | `-- value` |      4 |
| `OUT` | `0x61` | `port` | `value --` |      4 |
| `INT` | `0x70` | `n` | программное прерывание |      2 |
| `IRET` | `0x71` | — | возврат из обработчика прерывания |      2 |
| `EI` | `0x72` | — | разрешить аппаратные прерывания |      2 |
| `DI` | `0x73` | — | запретить аппаратные прерывания |      2 |

---

### 5.3. Порты ввода-вывода

Ввод-вывод реализован через порты:

| Порт | Назначение |
|---:|---|
| `0` | входной поток символов |
| `1` | выходной поток символов |
| `2` | выходной поток целых чисел |

`IN 0` читает один символ из входного потока и кладёт его код на стек.

`OUT 1` снимает значение со стека и добавляет младший байт в выходной поток как символ.

`OUT 2` снимает значение со стека, интерпретирует его как 32-битное знаковое целое число и добавляет его десятичное текстовое представление в выходной поток.

Если входной поток закончился, машина останавливается с причиной:

```text
Input stream exhausted
```

Для программы [cat](goldn/cat.yml) это нормальное завершение:

Она бесконечно читает и печатает символы, пока не закончится входной файл.

---

## 6. Транслятор

### 6.1. Назначение

`translator.py` переводит Lisp-программу в машинный код для стекового процессора.

---

### 6.2. Этапы трансляции

```text
source.lisp
    ↓
tokenize(source)
    ↓
Parser.parse_program()
    ↓
list[Expression]
    ↓
Compiler.compile_program()
    ↓
list[Instruction]
    ↓
write_code()       → .bin
write_disasm()     → .disasm
json(data_memory)  → .data.json
variables          → .vars
```

---

### 6.3. Пример трансляции `setq`

Исходный код:

```lisp
(setq x 1)
```

После парсера:

```python
[["setq", "x", 1]]
```

После трансляции примерно:

```text
0000: JMP 4
0004: PUSHI 1
0008: DUP
0012: STORE 8
0016: DROP
0020: HALT
```

`DROP` добавляется после top-level выражения, потому что результат выражения больше не нужен.

---

### 6.4. Процедуры

Процедуры объявляются так:

```lisp
(proc hello
  (print "Hello"))

(call hello)
```

Также поддерживаются параметры и рекурсивные вызовы:

```lisp
(proc fact (n)
  (if (= n 0)
    1
    (* n (call fact (- n 1)))))

(print-int (call fact 5))
```

Процедуры:

- могут иметь параметры;
- могут вызывать сами себя рекурсивно;
- возвращают результат последнего выражения тела;
- используют `CALL` / `RET` и стек возвратов;
- параметры хранятся в памяти данных в служебных ячейках транслятора.

Тело процедуры размещается в памяти команд до основного кода. Если тело пустое, процедура возвращает `0`. Если тело непустое, транслятор оставляет на стеке результат последнего выражения и добавляет `RET`.

При вызове процедуры с параметрами транслятор:

```text
1. сохраняет старые значения параметров на стек;
2. вычисляет новые аргументы;
3. записывает аргументы в ячейки параметров;
4. выполняет CALL addr;
5. сохраняет результат вызова во временную ячейку;
6. восстанавливает старые значения параметров;
7. возвращает результат вызова на стек.
```

Это позволяет рекурсивным вызовам не затирать значения параметров внешних вызовов.

Вызовы процедур используют `CALL addr`. Если процедура объявлена ниже места вызова, адрес патчится в конце трансляции через список `pending_calls`.

---

### 6.5. Условия

```lisp
(if condition then_expr else_expr)
```

Компилируется по схеме:

```text
compile condition
JZ else
compile then_expr
JMP end
else:
compile else_expr
end:
```

Условие считается ложным, если на стеке лежит `0`. Любое ненулевое значение считается истиной.

---

### 6.6. Цикл

```lisp
(loop condition
  body1
  body2
  ...)
```

Компилируется по схеме:

```text
loop_start:
    compile condition
    JZ loop_end
    compile body1
    DROP
    compile body2
    DROP
    ...
    JMP loop_start
loop_end:
    PUSHI 0
```

Цикл возвращает `0` после завершения.

---

## 7. Машина и микрокод

### 7.1. `isa.py`

`isa.py` задаёт систему команд:

- список опкодов;
- формат инструкции;
- проверку операндов;
- кодирование инструкции в 32-битное слово;
- декодирование 32-битного слова обратно в инструкцию;
- запись `.bin`;
- чтение `.bin`;
- генерацию `.disasm`.

---

### 7.2. `microcode.py`

`microcode.py` описывает, как каждая инструкция ISA раскладывается на микрооперации.

Пример для `ADD`:

```text
ADD:
    POP_A
    POP_B
    ALU_ADD
    PUSH_ALU
    INC_PC
    END_INSTRUCTION
```

---

### 7.3. `machine.py`

`machine.py` является симулятором процессора.

---

### 7.4. DataPath

<p align="center">
  <img src="schema/data path.png" alt="DataPath" width="850">
</p>

В модели используются следующие основные компоненты:

- `data_stack` — основной стек данных;
- `data_memory` — память данных;
- `input_buffer` — входной поток, связанный с портом `0`;
- `output_buffer` — общий выходной поток, в который пишут порты `1` и `2`;
- `reg_a` и `reg_b` — внутренние регистры операндов АЛУ;

Процессор является стековым, поэтому почти все операции выполняются через `data_stack`. Верхушка стека — последний элемент списка `data_stack`.

#### Stack mux

`stack mux` выбирает источник значения, которое должно попасть на стек данных. Возможные источники:

```text
IR operand      -> для PUSHI
data memory     -> для LOAD / LOADI
input buffer    -> для IN
ALU result      -> для ADD / SUB / MUL / DIV / MOD / сравнений
```

Выбор источника задаётся сигналами из ControlUnit. На схеме это показано пунктирной стрелкой `sel stack`, этот сигнал подает Control Unit.

#### Работа с портами

Ввод-вывод реализован через port-mapped I/O.

| Порт | Назначение |
|---|---|
| `0` | ввод символов |
| `1` | вывод символов |
| `2` | вывод целых чисел |

Инструкция:

```asm
IN 0
```

читает один символ из `input_buffer` и кладёт его ASCII-код на стек.

Инструкция:

```asm
OUT 1
```

снимает значение со стека и записывает младшие 8 бит в `output_buffer` как символ.

Инструкция:

```asm
OUT 2
```

снимает значение со стека и записывает его десятичное представление в `output_buffer`.

#### Return stack

`return_stack` хранит адреса возврата из процедур. Он используется только ControlUnit при выполнении инструкций `CALL` и `RET`.

При `CALL addr`:

```text
return_stack.push(PC + 4)
PC = addr
```

При `RET`:

```text
PC = return_stack.pop()
```

Пользовательская программа напрямую не работает с `return_stack`.

#### АЛУ и флаги

АЛУ выполняет арифметические операции и сравнения:

```text
ADD, SUB, MUL, DIV, MOD
EQ, NE, LT, GT, LE, GE
```

Операнды берутся из регистров `A` и `B`, результат записывается в `alu_result`, после чего может быть помещён обратно на стек через микрооперацию `PUSH_ALU`.

Флаги:

- `zero_flag` устанавливается, если результат АЛУ равен `0`;
- `negative_flag` устанавливается, если результат АЛУ меньше `0`.

---

### 7.5. ControlUnit

<p align="center">
  <img src="schema/control unit.png" alt="Control Unit" width="850">
</p>

ControlUnit содержит следующие основные элементы:

- `PC` — program counter, адрес текущей инструкции;
- `instruction memory` — память команд;
- `IR` — instruction register, регистр текущей инструкции;
- `opcode` — код операции из `IR`;
- `operand` — операнд из `IR`;
- `microprogram memory` — память микропрограмм;
- `MPC` — microprogram counter, счётчик микроопераций;
- `control logic` — блок формирования управляющих сигналов;
- `return_stack` — стек адресов возврата для `CALL` / `RET`.

#### Общий цикл работы

Процессор работает по тактам. Один `FETCH` занимает один такт, и каждая микрооперация также занимает один такт.

Общий цикл работы ControlUnit:

```text
if IR is None:
    check interrupt
    if interrupt is pending:
        enter interrupt handler
    else:
        fetch instruction at byte address PC
        load IR
        select microprogram by opcode
        MPC = 0
else:
    execute current_microprogram[MPC]
```

#### Microprogram Counter

`MPC` указывает, какая микрооперация текущей микропрограммы выполняется сейчас.

После большинства микроопераций `MPC` увеличивается на `1`.

Когда выполняется `END_INSTRUCTION`, ControlUnit очищает `IR`, сбрасывает `MPC` и на следующем такте снова переходит к `FETCH`.

#### Control logic

`control logic` получает текущую микрооперацию из `microprogram memory` и формирует управляющие сигналы для DataPath.

На схеме выход `signals` из ControlUnit идёт в DataPath. Эти сигналы управляют стеком, памятью данных, АЛУ, мультиплексорами и вводом/выводом.

---

## 8. Тестирование

### 0. Формат `golden.yml` файлов

`golden.yml` используется для golden-тестов.  

Общая структура

```yaml
in_source: |-
  исходный код программы на Lisp

in_stdin: |-
  входные данные программы

in_interrupt_ticks: |-
  на каком тике вызывать прерывание

in_interrupt_symbol: |-
  символ прерывания

out_code_hex: |-
  ожидаемый дизассемблер

out_data_dec: |-
  ожидаемая начальная память данных в формате слов по 4 байта:
  01 -   0,   0,   0,   0
  02 -   0,   0,   0,   0

out_stdout: |
  ожидаемый stdout после запуска

out_log: |-
  ожидаемый лог выполнения
```

Все тесты выполняются примерно за 90 секунд (такое время из-за программы prob1).

### 1. [hello](goldn/hello.yml)

Напечатать Hello World!

### 2. [cat](goldn/cat.yml)

Печатать данные, поданные через ввод (размер ввода потенциально бесконечен).

### 3. [hello_user_name](goldn/hello_user_name.yml)

Запросить у пользователя его имя, считать его, вывести на экран приветствие.

### 4. [sort](goldn/sort.yml)

Пользователь подаёт во входной поток количество чисел `n`, затем сами числа. Программа считывает их через `read-int`, сохраняет в массив в памяти данных с шагом 4 байта, сортирует и выводит в отсортированном формате.

### 5. [double_precision](goldn/double_precision.yml)

Программа демонстрирует сложение чисел, которые не помещаются в одно машинное слово.

Алгоритм операций такой:

- Число представляется двумя частями в базе `10000`:

  ```text
  value = hi * BASE + lo
  BASE = 10000
  ```

- Исходные значения:

  ```text
  a = 4294967296 = 429496 * 10000 + 7296
  b = 4294967296 = 429496 * 10000 + 7296
  ```

- Сначала складываются младшие части;

- Если `sum_lo >= BASE`, выполняется перенос;

- Затем складываются старшие части с учётом переноса;

- Получение итогового значения;

- При выводе сначала печатается `sum_hi`, затем `sum_lo`;

- Младшая часть должна занимать ровно 4 цифры, поэтому перед `sum_lo` при необходимости печатаются ведущие нули;

- Вывод программы.

### 6. [prob1](goldn/prob1.yml)

Нужно найти максимальный палиндром, который получается при произведении двух трёхзначных чисел.

Можно заметить, что любой шестизначный палиндром имеет вид:

```text
abccba
```

Тогда его можно представить так:

```text
abccba =
100000a + 10000b + 1000c +
100c + 10b + a
```

Сгруппируем слагаемые:

```text
= 100001a + 10010b + 1100c
```

Вынесем общий множитель `11`:

```text
= 11 * (9091a + 910b + 100c)
```

Следовательно, любой шестизначный палиндром делится на `11`.

Значит, в произведении двух трёхзначных чисел хотя бы один множитель должен делиться на `11`, что используется в программе для сокращения количества проверок.

### 7. [show_string_methods](goldn/show_string_methods.yml)

Показывает различные вариант работы со строками:

- Присваивание переменной строки и печать;

- Печать без присваивания;

- Получение длины строки;

- Печать символа строки с индексом `i`;

- Замена символа в строке под индексом `i` на другой.

### 8. [factorial](goldn/factorial.yml)

Реализован факториал через рекурсию.

### 9. [interrupt_test](goldn/interrupt_test.yml)

Показано как работает прерывание в языке. 

- Объявляет обработчик прерывания: выводит строку "INT:", символ из `data_memory[0x04]` и перевод строки;
- Включает прерывания через (enable-interrupts);
- Запускает основной цикл со счётчиком i от 0 до 4;
- В цикле выводит текущее значение i, перевод строки и увеличивает i на 1;
- При аппаратном прерывании выполнение цикла временно приостанавливается, выполняется обработчик, затем программа продолжает работу с того же места;
- Прерывания задаются при запуске через список тактов, например --interrupt-ticks "500,1000".

---


## 9. Файлы, создаваемые при запуске

Удобный запуск выполняется через `run_program.py`:

```bash
python run_program.py examples/hello.lisp
```

Для программы `examples/hello.lisp` будут созданы файлы:

```text
build/hello.bin            ; бинарный машинный код
build/hello.data.json      ; начальная память данных
build/hello.disasm         ; человекочитаемый дизассемблер
build/hello.vars           ; карта переменных
build/hello.out            ; вывод программы
build/hello.log            ; журнал тактового выполнения
build/hello.data.out.json  ; финальное состояние памяти данных
```

`.bin` читает `machine.py`.  
`.disasm` нужен человеку для отладки.  
`.data.json` содержит начальное состояние памяти данных в байтовом виде.  
`.data.out.json` показывает память после завершения программы тоже в байтовом виде.

---

## 10. Интерфейс командной строки

### 10.1. Транслятор

```bash
python translator.py source.lisp output.bin --data output.data.json --disasm output.disasm --vars output.vars
```

Аргументы:

| Аргумент | Назначение |
|---|---|
| `source.lisp` | исходный файл |
| `output.bin` | бинарный файл инструкций |
| `--data` | файл начальной памяти данных |
| `--disasm` | дизассемблер |
| `--vars` | карта переменных |

---

### 10.2. Машина

```bash
python machine.py output.bin --data output.data.json --input input.txt --output output.txt --log output.log
```

Аргументы:

| Аргумент | Назначение |
|---|---|
| `code` | бинарный файл инструкций |
| `--data` | начальная память данных |
| `--input` | входной поток |
| `--output` | файл вывода |
| `--log` | журнал тактов |
| `--data-output` | финальная память данных |
| `--tick-limit` | лимит тактов |
| `--data-memory-size` | размер памяти данных |
| `--interrupt-ticks` | такты аппаратных прерываний |
| `--interrupt-symbol` | символ устройства, помещаемый в `data_memory[0x04]` |

---

### 10.3. Скрипт полного запуска

```bash
python run_program.py examples/program.lisp --input examples/program.in
```

Пример с `cat`:

```bash
python run_program.py examples/cat.lisp --input examples/cat.in
```

Пример с прерываниями:

```bash
python run_program.py examples/interrupt.lisp --interrupt-ticks 50 --interrupt-symbol '!'
```

---

## 11. Формат дизассемблера

Пример `.disasm`:

```text
0000 - 40000004 - JMP 4
0004 - 01000001 - PUSHI 1
0008 - 03000000 - DUP
0012 - 11000008 - STORE 8
0016 - 02000000 - DROP
0020 - 00000000 - HALT
```

Формат строки:

```text
адрес - машинное слово в hex - мнемоника операнд
```

---

## 12. Журнал выполнения

Каждый такт записывается в `.log`.

Пример строки:

```text
TICK=000001 | PC=0 | MPC=0 | IR=None | MICRO=FETCH | A=0 | B=0 | ALU=0 | ZF=0 | NF=0 | STACK=[] | IE=0 | IN_INT=0 | INT=- | INT_COUNT=0 | DEV=0 | RET=[] | OUT=''
```

Поля:

| Поле | Значение |
|---|---|
| `TICK` | номер такта |
| `PC` | адрес текущей инструкции |
| `MPC` | адрес микрооперации внутри микропрограммы |
| `IR` | текущая инструкция |
| `MICRO` | выполненная микрооперация |
| `A`, `B`, `ALU` | внутренние регистры АЛУ |
| `ZF`, `NF` | флаги результата АЛУ |
| `STACK` | верхние элементы стека данных |
| `IE` | разрешены ли аппаратные прерывания |
| `IN_INT` | выполняется ли обработчик прерывания |
| `INT` | последнее событие прерывания |
| `INT_COUNT` | количество обработанных прерываний |
| `DEV` | значение interrupt device cell |
| `RET` | стек возвратов |
| `OUT` | накопленный вывод |

---